version: '3.9'

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sqlserver
    environment:
      SA_PASSWORD: "kotik"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    networks:
      - messenger-net

  userservice:
    build:
      context: ./UserService
    ports:
      - "5001:80"
    depends_on:
      - sqlserver
    networks:
      - messenger-net

  messageservice:
    build:
      context: ./MessageService
    ports:
      - "5002:80"
    depends_on:
      - sqlserver
    networks:
      - messenger-net

networks:
  messenger-net:
    driver: bridge
